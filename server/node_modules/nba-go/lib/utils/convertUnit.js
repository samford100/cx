
const convertToCm = length => {
  if (length !== '') {
    var _length$split = length.split('-'),
        _length$split2 = _slicedToArray(_length$split, 2);

    const foot = _length$split2[0],
          inches = _length$split2[1];


    return (foot * 30.48 + inches * 2.54).toFixed(2);
  }

  return '';
};

const convertToKg = weight => weight !== '' ? (weight * 0.45359237).toFixed(2) : '';

exports.convertToCm = convertToCm;
exports.convertToKg = convertToKg;
//# sourceMappingURL=convertUnit.js.map